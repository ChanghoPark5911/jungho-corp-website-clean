# 정호그룹 홈페이지 디자인 시스템 가이드

> **Version:** 1.0  
> **Last Updated:** 2026-01-20  
> **Framework:** React + Tailwind CSS  

이 문서는 정호그룹 홈페이지의 디자인 일관성을 유지하기 위한 가이드입니다.  
향후 작업 시 이 문서를 참조하여 동일한 스타일을 적용해주세요.

---

## 📋 목차

1. [색상 팔레트](#1-색상-팔레트-color-palette)
2. [타이포그래피](#2-타이포그래피-typography)
3. [간격 시스템](#3-간격-시스템-spacing)
4. [반응형 브레이크포인트](#4-반응형-브레이크포인트-breakpoints)
5. [컴포넌트 스타일](#5-컴포넌트-스타일-components)
6. [애니메이션 & 인터랙션](#6-애니메이션--인터랙션)
7. [특수 효과](#7-특수-효과-special-effects)
8. [주요 섹션 스타일](#8-주요-섹션-스타일)

---

## 1. 색상 팔레트 (Color Palette)

### 1.1 Primary Colors (메인 그린)

정호그룹의 브랜드 컬러는 **그린 계열**입니다.

| 이름 | HEX | Tailwind | 용도 |
|------|-----|----------|------|
| primary-50 | `#E8F5E9` | `bg-primary-50` | 매우 연한 배경 |
| primary-100 | `#C8E6C9` | `bg-primary-100` | 연한 배경, hover 상태 |
| primary-200 | `#A5D6A7` | `bg-primary-200` | 선택 영역 배경 |
| primary-300 | `#81C784` | `bg-primary-300` | 스크롤바, 보조 요소 |
| primary-400 | `#66BB6A` | `bg-primary-400` | 활성 상태 |
| **primary-500** | `#4CAF50` | `bg-primary-500` | **Accent Green (기본 강조)** |
| **primary-600** | `#2E7D32` | `bg-primary-600` | **Primary Green (메인 브랜드)** |
| primary-700 | `#1B5E20` | `bg-primary-700` | Dark Green (진한 강조) |
| primary-800 | `#145A1C` | `bg-primary-800` | 매우 진한 배경 |
| primary-900 | `#0D3F12` | `bg-primary-900` | 가장 진한 배경 |

### 1.2 Accent Colors (강조 레드)

| 이름 | HEX | Tailwind | 용도 |
|------|-----|----------|------|
| accent-50 | `#FEF2F2` | `bg-accent-50` | 에러 배경 (연한) |
| accent-100 | `#FEE2E2` | `bg-accent-100` | 경고 배경 |
| **accent-500** | `#EF4444` | `bg-accent-500` | **Main Accent Red** |
| accent-600 | `#DC2626` | `bg-accent-600` | 버튼 hover |
| accent-700 | `#B91C1C` | `bg-accent-700` | 진한 경고 |

### 1.3 Neutral Colors (중립색)

| 이름 | HEX | Tailwind | 용도 |
|------|-----|----------|------|
| neutral-50 | `#F9FAFB` | `bg-gray-50` | 기본 배경색 |
| neutral-100 | `#F3F4F6` | `bg-gray-100` | 섹션 배경 |
| neutral-200 | `#E5E7EB` | `bg-gray-200` | 보더, 구분선 |
| neutral-300 | `#D1D5DB` | `bg-gray-300` | 비활성 상태 |
| neutral-500 | `#6B7280` | `text-gray-500` | 보조 텍스트 |
| neutral-700 | `#374151` | `text-gray-700` | 본문 텍스트 |
| neutral-800 | `#1F2937` | `text-gray-800` | 제목 텍스트 |
| neutral-900 | `#111827` | `bg-gray-900` | 다크모드 배경 |

### 1.4 계열사별 브랜드 컬러

| 계열사 | HEX | Tailwind | 설명 |
|--------|-----|----------|------|
| **클라루스** | `#00897B` | `bg-clarus-500` | 청록 그린 (Tech) |
| **정호티엘씨** | `#2E7D32` | `bg-tlc-500` | 메인 그린 |
| **일루텍** | `#FFA726` | `bg-illutech-500` | 오렌지 (LED 이미지) |
| **정호텍스컴** | `#7E57C2` | `bg-texcom-500` | 퍼플 (패션 이미지) |

### 1.5 CSS 변수 사용법

```css
/* CSS 변수 직접 사용 */
.element {
  color: var(--primary-600);
  background-color: var(--neutral-50);
}
```

---

## 2. 타이포그래피 (Typography)

### 2.1 폰트 패밀리

```css
/* 기본 폰트 (한글) */
--font-family-korean: 'Pretendard Variable', 'Pretendard', 'Noto Sans KR', sans-serif;

/* 영문 폰트 */
--font-family-english: 'Pretendard Variable', 'Pretendard', 'Montserrat', sans-serif;
```

**Tailwind 클래스:**
- `font-korean` - 한글 폰트
- `font-english` - 영문 폰트
- `font-pretendard` - Pretendard 기본

### 2.2 폰트 굵기 (Font Weight)

| 용도 | 값 | CSS 변수 | Tailwind |
|------|-----|----------|----------|
| 히어로 Light | 200 | - | `font-extralight` |
| Light | 300 | `--font-light` | `font-light` |
| Regular | 400 | `--font-regular` | `font-normal` |
| Medium | 500 | `--font-medium` | `font-medium` |
| Semibold | 600 | `--font-semibold` | `font-semibold` |
| Bold | 700 | `--font-bold` | `font-bold` |
| **Extra Bold** | 900 | - | `font-black` |

**✨ 히어로 슬로건 폰트 대비 (핵심)**
```jsx
// '40년의 신뢰' - Light (200)
<span style={{ fontWeight: 200 }}>40년의 신뢰</span>

// '정호그룹이 밝히는 미래' - Extra Bold (900)
<span style={{ fontWeight: 900 }}>정호그룹이 밝히는 미래</span>
```

### 2.3 폰트 크기 (반응형)

| 요소 | Mobile | Tablet | Desktop | Tailwind |
|------|--------|--------|---------|----------|
| Hero | 24px | 32px | 40px | `text-hero-mobile md:text-hero-tablet lg:text-hero-desktop` |
| H1 | 24px | 32px | 40px | `text-2xl md:text-3xl lg:text-4xl` |
| H2 | 20px | 24px | 32px | `text-xl md:text-2xl lg:text-3xl` |
| H3 | 18px | 20px | 24px | `text-lg md:text-xl lg:text-2xl` |
| Body | 14px | 16px | 16px | `text-sm md:text-base` |
| Small | 12px | 14px | 14px | `text-xs md:text-sm` |

### 2.4 행간 (Line Height)

| 용도 | 값 | CSS | Tailwind |
|------|-----|-----|----------|
| 제목 (타이트) | 1.0~1.2 | `line-height: 1.1` | `leading-tight` |
| 본문 | 1.6~1.7 | `line-height: 1.6` | `leading-relaxed` |
| 넓은 행간 | 1.8~2.0 | `line-height: 1.8` | `leading-loose` |

---

## 3. 간격 시스템 (Spacing)

### 3.1 기본 간격 (8px 베이스)

| 변수 | 값 | Tailwind |
|------|-----|----------|
| `--space-1` | 8px | `p-2`, `m-2` |
| `--space-2` | 16px | `p-4`, `m-4` |
| `--space-3` | 24px | `p-6`, `m-6` |
| `--space-4` | 32px | `p-8`, `m-8` |
| `--space-6` | 48px | `p-12`, `m-12` |
| `--space-8` | 64px | `p-16`, `m-16` |
| `--space-10` | 80px | `p-20`, `m-20` |
| `--space-12` | 96px | `p-24`, `m-24` |

### 3.2 섹션 패딩 (수직 여백)

**기업용 여유로운 레이아웃 (1.5배 증가 적용됨)**

| 화면 크기 | 상하 패딩 | Tailwind |
|----------|----------|----------|
| Mobile | 64px (4rem) | `py-16` |
| Tablet | 96px (6rem) | `md:py-24` |
| Desktop | 128px (8rem) | `lg:py-32` |

```jsx
// 기본 섹션 클래스
<section className="py-16 md:py-24 lg:py-32 px-6 md:px-10 lg:px-16">
```

### 3.3 컨테이너 여백 (좌우 마진)

| 화면 크기 | 좌우 패딩 | Tailwind |
|----------|----------|----------|
| Mobile | 24px | `px-6` |
| Tablet | 40px | `md:px-10` |
| Desktop | 64px | `lg:px-16` |
| Large | 80px | `xl:px-20` |

```jsx
// 기본 컨테이너 클래스
<div className="max-w-6xl mx-auto px-6 md:px-10 lg:px-16 xl:px-20">
```

### 3.4 헤더 높이

| 화면 크기 | 높이 | Tailwind |
|----------|------|----------|
| Mobile | 72px (4.5rem) | `h-18` |
| Tablet | 88px (5.5rem) | `sm:h-22` |
| Desktop | 112px (7rem) | `lg:h-28` |

---

## 4. 반응형 브레이크포인트 (Breakpoints)

| 이름 | 최소 너비 | Tailwind Prefix | 설명 |
|------|----------|-----------------|------|
| xs | 320px | `xs:` | 소형 모바일 |
| sm | 640px | `sm:` | 모바일 |
| **md** | 768px | `md:` | **모바일 끝점** |
| **lg** | 1024px | `lg:` | **태블릿 끝점** |
| **xl** | 1280px | `xl:` | **데스크톱 시작점** |
| 2xl | 1536px | `2xl:` | 대형 데스크톱 |
| 3xl | 1920px | `3xl:` | 초대형 |
| 4xl | 2560px | `4xl:` | 4K |

---

## 5. 컴포넌트 스타일 (Components)

### 5.1 버튼 시스템

```jsx
// Primary 버튼
<button className="btn-primary">확인</button>

// Secondary 버튼 (아웃라인)
<button className="btn-secondary">취소</button>

// Accent 버튼 (레드)
<button className="btn-accent">삭제</button>

// 계열사별 버튼
<button className="btn-clarus">클라루스</button>
<button className="btn-tlc">정호티엘씨</button>
<button className="btn-illutech">일루텍</button>
<button className="btn-texcom">텍스컴</button>
```

**버튼 크기:**
- `btn-sm` - 작은 버튼 (padding: 8px 16px)
- `btn-md` - 기본 버튼 (padding: 16px 32px)
- `btn-lg` - 큰 버튼 (padding: 24px 48px)
- `btn-xl` - 매우 큰 버튼 (padding: 32px 64px)

### 5.2 카드 시스템

```jsx
// 기본 카드
<div className="card">내용</div>

// 계열사별 카드 (왼쪽 컬러 보더)
<div className="card card-clarus">클라루스</div>
<div className="card card-tlc">정호티엘씨</div>
<div className="card card-illutech">일루텍</div>
<div className="card card-texcom">텍스컴</div>
```

**카드 스타일:**
- border-radius: 12px (`rounded-xl`)
- Hover 시 -8px 위로 이동 (`hover:-translate-y-2`)
- Hover 시 그림자 깊어짐 (`hover:shadow-2xl`)
- 전환 시간: 400ms

### 5.3 섹션 & 컨테이너

```jsx
// 기본 섹션
<section className="section">
  <div className="container">
    내용
  </div>
</section>

// 기업용 넓은 여백 컨테이너
<div className="container-corporate">내용</div>
```

### 5.4 그리드 시스템

```jsx
// 반응형 그리드 (1→2→3→4 컬럼)
<div className="grid-responsive">
  <div>Item 1</div>
  <div>Item 2</div>
  <div>Item 3</div>
  <div>Item 4</div>
</div>

// 고정 컬럼 그리드
<div className="grid-2">2컬럼</div>
<div className="grid-3">3컬럼</div>
<div className="grid-4">4컬럼</div>
```

---

## 6. 애니메이션 & 인터랙션

### 6.1 기본 애니메이션

| 이름 | Tailwind | 설명 |
|------|----------|------|
| Fade In | `animate-fade-in` | 0.5초 페이드 인 |
| Slide Up | `animate-slide-up` | 아래에서 위로 슬라이드 |
| Slide Down | `animate-slide-down` | 위에서 아래로 슬라이드 (0.3초) |
| Scale In | `animate-scale-in` | 확대되며 나타남 |
| Bounce Slow | `animate-bounce-slow` | 느린 바운스 (2초) |
| Pulse Slow | `animate-pulse-slow` | 느린 펄스 (3초) |

### 6.2 전환 시간 (Duration)

| 용도 | 값 | CSS 변수 |
|------|-----|----------|
| 빠른 전환 | 150ms | `--duration-fast` |
| 기본 전환 | 300ms | `--duration-normal` |
| 느린 전환 | 500ms | `--duration-slow` |

### 6.3 Hover 효과

```jsx
// 위로 살짝 이동
<div className="hover-lift">Hover me</div>

// 그린 글로우 효과
<div className="hover-glow">Hover me</div>

// 확대 효과
<div className="hover-scale">Hover me</div>

// 회전 효과
<div className="hover-rotate">Hover me</div>
```

### 6.4 스크롤 트리거 애니메이션

```jsx
// 스크롤 시 페이드 인
<div className="scroll-fade-in">스크롤하면 나타남</div>

// 왼쪽에서 슬라이드
<div className="scroll-slide-left">왼쪽에서</div>

// 오른쪽에서 슬라이드
<div className="scroll-slide-right">오른쪽에서</div>

// 확대되며 나타남
<div className="scroll-scale-in">확대</div>
```

### 6.5 Framer Motion 패턴

```jsx
import { motion } from 'framer-motion';

// 기본 애니메이션
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  내용
</motion.div>

// 뷰포트 진입 시 애니메이션
<motion.div
  initial={{ opacity: 0, y: 50 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, amount: 0.3 }}
  transition={{ duration: 0.6 }}
>
  스크롤 시 나타남
</motion.div>

// Hover 시 위로 이동
<motion.div whileHover={{ y: -8 }}>
  카드
</motion.div>
```

---

## 7. 특수 효과 (Special Effects)

### 7.1 Glassmorphism (유리 효과)

**헤더에 적용된 스타일:**

```jsx
<header
  className="bg-white/85 dark:bg-gray-900/85"
  style={{
    backdropFilter: 'blur(10px) saturate(150%)',
    WebkitBackdropFilter: 'blur(10px) saturate(150%)',
  }}
>
```

**드롭다운 메뉴:**
```jsx
<div
  className="bg-white/95 dark:bg-gray-800/95 backdrop-blur-xl"
  style={{
    backdropFilter: 'blur(20px) saturate(180%)',
    WebkitBackdropFilter: 'blur(20px) saturate(180%)',
  }}
>
```

### 7.2 그라데이션

```jsx
// 기본 그라데이션
<div className="bg-gradient-primary">Primary 그라데이션</div>
<div className="bg-gradient-accent">Accent 그라데이션</div>
<div className="bg-gradient-dark">Dark 그라데이션</div>

// 커스텀 그라데이션
<div className="bg-gradient-to-r from-blue-500 to-emerald-500">
  왼쪽에서 오른쪽으로
</div>
```

### 7.3 그림자 (Shadows)

| 이름 | CSS 변수 | 용도 |
|------|----------|------|
| Small | `--shadow-sm` | 버튼, 작은 요소 |
| Medium | `--shadow-md` | 카드 기본 |
| Large | `--shadow-lg` | 카드 hover |
| Brand Green | `--shadow-brand-green` | 그린 글로우 |
| Accent Red | `--shadow-accent-red` | 레드 글로우 |

```jsx
<div className="shadow-brand">그린 글로우</div>
<div className="shadow-accent">레드 글로우</div>
```

### 7.4 Text Shadow (히어로 섹션)

```jsx
// Light 텍스트
style={{ textShadow: '0 2px 15px rgba(0, 0, 0, 0.4)' }}

// Bold 텍스트
style={{ textShadow: '0 4px 30px rgba(0, 0, 0, 0.5), 0 2px 15px rgba(0, 0, 0, 0.3)' }}
```

---

## 8. 주요 섹션 스타일

### 8.1 헤더 (MegaMenu)

```jsx
<header 
  className={`
    fixed top-0 left-0 right-0 z-[100] 
    transition-all duration-500 ease-out
    ${isScrolled 
      ? 'bg-white/85 shadow-lg border-b border-gray-200/30' 
      : 'bg-white/60 shadow-sm'
    }
  `}
  style={{
    backdropFilter: 'blur(10px) saturate(150%)',
    WebkitBackdropFilter: 'blur(10px) saturate(150%)',
  }}
>
```

**메뉴 Hover 언더라인:**
```jsx
<span 
  className={`
    absolute bottom-1 left-1/2 -translate-x-1/2 
    h-0.5 bg-gradient-to-r from-primary-500 to-primary-600
    transition-all duration-300 ease-out
    ${activeMenu === menu.id ? 'w-4/5' : 'w-0 group-hover:w-4/5'}
  `}
/>
```

### 8.2 히어로 섹션 (SmallBanner)

**슬로건 스타일:**
```jsx
{/* Light - '40년의 신뢰' */}
<span 
  className="text-white/85 text-xl md:text-2xl lg:text-3xl xl:text-4xl tracking-widest uppercase"
  style={{ fontWeight: 200, letterSpacing: '0.25em' }}
>
  40년의 신뢰
</span>

{/* Extra Bold - '정호그룹이 밝히는 미래' */}
<span 
  className="text-white text-4xl md:text-5xl lg:text-6xl xl:text-7xl 2xl:text-8xl tracking-tight"
  style={{ fontWeight: 900, letterSpacing: '-0.02em', lineHeight: '1.0' }}
>
  정호그룹이 밝히는 미래
</span>
```

**배경 오버레이:**
```jsx
<div className="absolute inset-0 bg-gradient-to-b from-black/40 via-black/30 to-black/60" />
<div className="absolute inset-0 bg-gradient-to-r from-blue-900/20 via-transparent to-emerald-900/20" />
```

### 8.3 계열사 카드 섹션

```jsx
<motion.div 
  className="group bg-white border-2 border-gray-200 rounded-xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-400 cursor-pointer"
  whileHover={{ y: -8 }}
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5, delay: index * 0.1 }}
>
```

### 8.4 숫자 카운팅 섹션

```jsx
import CountUp from 'react-countup';

<CountUp 
  end={40} 
  suffix="+" 
  duration={2.5} 
  enableScrollSpy 
  scrollSpyOnce 
/>
```

### 8.5 인증/특허 마크 (Grayscale to Color)

```jsx
<img 
  className="filter grayscale opacity-60 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-500"
/>
```

---

## 📌 Quick Reference

### 자주 사용하는 클래스 조합

```jsx
// 기본 페이지 레이아웃
<main className="pt-28 min-h-screen bg-gray-50 dark:bg-gray-900">
  <section className="py-16 md:py-24 lg:py-32">
    <div className="max-w-6xl mx-auto px-6 md:px-10 lg:px-16">
      {/* 내용 */}
    </div>
  </section>
</main>

// 반응형 그리드 카드
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
  {items.map((item) => (
    <motion.div 
      className="bg-white rounded-xl p-6 shadow-md hover:shadow-2xl transition-all"
      whileHover={{ y: -8 }}
    >
      {/* 카드 내용 */}
    </motion.div>
  ))}
</div>

// 섹션 제목
<h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-4">
  섹션 제목
</h2>
<p className="text-lg text-gray-600 dark:text-gray-400">
  섹션 설명
</p>
```

---

## 🔧 설정 파일 위치

| 파일 | 역할 |
|------|------|
| `tailwind.config.js` | Tailwind 커스텀 설정 (색상, 간격, 폰트) |
| `src/index.css` | CSS 변수, 컴포넌트 클래스, 유틸리티 |
| `public/index.html` | Pretendard 폰트 CDN 링크 |

---

**🚨 주의사항:**
- 새로운 색상 추가 시 `tailwind.config.js`와 `src/index.css` 모두 업데이트
- 반응형 작업 시 Mobile First 원칙 준수
- 다크모드 지원을 위해 `dark:` prefix 사용
- 애니메이션은 `prefers-reduced-motion` 접근성 고려
